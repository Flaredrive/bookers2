<h1>New bookこれはイラン</h1>
<%= form_with model: @book, url: user_books_path(@user) do |f| %>
  <p>Title</p>
  <%= f.text_field :title %>
  <p>Opinion</p>
  <%= f.text_area :body %>
  <%= f.submit 'Create book' %>
<% end %>